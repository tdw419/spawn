{
  "name": "spawn",
  "version": "0.1.0",
  "description": "One-click setup wizard for AI agent environments",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "build:linux": "tauri build --bundles deb appimage",
    "build:windows": "tauri build --bundles msi nsis",
    "build:mac": "tauri build --bundles dmg app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
